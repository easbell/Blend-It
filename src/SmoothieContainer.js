import React, { Component } from 'react'

export class SmoothieContainer extends Component {
  render() {
    return (
      <div>
        {/* This will be the container for all reciepes showing */}
        {/* Maybe we want a conditional for if showing a set amount as default?? */}
      </div>
    )
  }
}

export default SmoothieContainer
